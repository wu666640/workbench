# 更新公告

## 1.3.0

- 新增“每日灵感”页面：每天一句中英金句，可手动换一句；重点新闻每天 15 条自动更新，断网也能看当天缓存。
- 设置里可开启“每日精句推送”并自定义时间，到点通过手机通知收到当天金句。

## 1.2.14

- 修复东秦课表导入“无权限”问题：教务入口改为一网通办门户 `eone.neuq.edu.cn`，登录后进入“教务管理系统/教学服务”的课表页，另存为 HTML 再上传即可；原有 xls 导入保持不变。

## 1.2.13

- 东北大学秦皇岛分校课表导入优化：新增“登录后打开课表页”按钮，点开后登录教务、把课表页另存为 HTML 再上传即可；原有 xls 导入保持不变。

## 1.2.12

- 课表导入新增学校选择：东北大学秦皇岛分校 / 哈尔滨工业大学，选择会自动保存。
- 哈工大 xls 课表按两节合并，老师、周次、地点都能识别，带哈工大样例。

## 1.2.11

- 修复东北大学 xls 课表识别：自动读取真正的课表工作表，课程按两节合并成一个大课块，同一格多门课也能正确拆开。

## 1.2.10

- 待办、习惯、课表、记录、资料库、自习室、复盘页面都新增“一键清空”按钮，点击后需要确认才会清空。

## 1.2.9

- 修复项目列表可能整个不显示的问题：即使数据库新字段还没更新，私人工作台和项目也会正常显示。

## 1.2.8

- 修复切换项目/私人工作台时个人数据被覆盖的问题，每个空间现在独立保存，不会互相串。
- 项目按钮当前所在空间会高亮，私人工作台和项目一眼能分清。
- 项目列表直接显示邀请码和剩余有效时间，邀请码 30 分钟过期，可一键重新生成。
- 加入项目时如果邀请码已过期，会明确提示。
- 需要重新运行一次更新后的 `multi_user_schema.sql`，加入邀请码过期字段。

## 1.2.7

- 用户名登录改为直接去云端查找旧账号对应邮箱，之前注册过的账号和新设备都能直接用用户名登录。
- 项目列表新增“私人工作台”，随时切回个人数据，不会再找不到。
- 支持删除项目，项目里的共享数据会一并清理。
- 创建项目的权限报错仍需在 Supabase SQL Editor 重新运行一次更新后的权限脚本。

## 1.2.6

- 修复创建项目时的 `project_members` 权限递归报错：需要把更新后的权限脚本重新在 Supabase SQL Editor 里运行一次。
- 手机底部导航对之前已设置显示的旧账号也会默认收起一次，之后仍可自己展开。
- 之前注册过的账号，打开一次新版后会自动记住用户名对应关系，单纯用用户名也能登录。

## 1.2.5

- 修复云端同步一直显示“已存本机”的问题：登录凭证会自动续期，过期后也会提示重新登录。
- 修复同时填写用户名和邮箱注册后，单纯用用户名无法登录的问题，会记住“用户名 → 邮箱”对应关系。
- 手机底部导航默认收起，界面更清爽，需要时可点顶部“展开导航”。

## 1.2.4

- 更新更稳：检查和下载安装包改到工作台自己的地址，不开梯子也能更快更新，开梯子也不容易失败。

## 1.2.3

- 空教室新增“区间筛选”：选择第几节到第几节，直接显示这段时间都没课的教室。
- 新增“全天无课”快捷键，一键筛选第 1~12 节都空闲的教室。

## 1.2.2

- 修复 App 内“检查更新”无法下载安装的问题，安卓改用系统原生网络下载，不再被网页跨域限制挡住。

## 1.2.1

- 打开 App 后先进入登录页，支持用用户名或邮箱注册登录。
- 注册账号会自动确认，不再需要去邮箱点验证链接，也不会被邮箱验证卡住。
- 注册后端由 Netlify 托管，Supabase 管理密钥只放在后端，不暴露给网页和手机。

## 1.2.0

- 支持哈工大 `.xls` 课表导入，并按学年/季节切换不同阶段课表。
- 自习室新增“截图导入”，用通义视觉模型识别教室名，本地检测圆点颜色判断占用。
- 支持 AI 识别配置，API Key 只保存在自己设备上。
- 新增账号登录、项目创建/加入/切换，多人可共享课表、自习室和任务。

## 1.1.3

- 支持直接导入哈工大 `.xls` 课表文件。
- 支持按学年和季节切换不同阶段课表，例如“2026春季学期”。
- 自动拆分同一格里的多门课程，并识别老师、周次和地点。
- 自动区分东北大学网页版课表和哈工大真实 Excel 课表。

## 1.1.2

- App 内新增“检查更新”，检测到新版本后可直接下载并跳转安装。
- 每次更新都会在安装前展示对应版本的更新公告。
- 课表自动把相邻两节合并成一个大课块。

## 1.1.1

- 修正课表导入，课程按连续两节合并显示。
- 保留上一版本下载链接，安装包不再覆盖旧版本。

## 1.1.0

- 待办和习惯支持选择具体时间。
- 可选择提前提醒时间，安卓手机版支持系统通知。
- 设置里新增提醒通知开关和测试提醒。

## 1.0.0

- 个人工作台首个正式版本。
- 网页版发布到 GitHub Pages，手机版打包为安卓 APK。
- 数据通过云端在网页、手机和 iPad 之间同步。
